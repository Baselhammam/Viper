M1 gate — all must clear:
- >=40% of real edits are tweaks (not full rewrites)
- >=95% of patches apply cleanly
- 60-80% fewer output tokens vs full regeneration
- quality >= regeneration on >=85% of cases
- latency to first op <= ~1.5s
